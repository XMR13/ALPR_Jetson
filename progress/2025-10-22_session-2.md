## Session Progress - 2025-10-22 (Session 2)

## Summary
- Addressed out-of-memory (OOM) on Jetson NX by adding low-memory controls to the ONNX OCR path and exposing them via CLI. You can now cap ONNXRuntime CUDA memory or switch OCR to CPU.
- Documented memory modes (capped vs uncapped) and recommended flags in README. Synced plan with a resource-tuning checkpoint.

## Changes
- Modified:
  - `src/ocr_service/onnx_infer.py` — add low-memory `SessionOptions`, provider selection (CUDA/CPU), and `gpu_mem_limit` cap via constructor.
  - `src/alpr_jetson/__main__.py` — add CLI flags `--onnx-provider cuda|cpu` and `--onnx-gpu-mem-limit-mb` (0 = uncapped) for both `ocr-infer` and `e2e`.
  - `README.md` — add “ONNX OCR — Mode Memori” section with examples (capped and uncapped) and guidance (raise `--conf`, omit `--annotate-dir`).
  - `plan.md` — Week 2 updated with “Resource tuning on Jetson NX” bullet and recommended flags.
- Added:
  - `progress/2025-10-22_session-2.md` — this log.
- Deleted: (none)

## Plan Snapshot (plan.md §5)
- Week 2 Day 8–9: OCR integration — checked (ONNX runner supports CUDA/CPU; memory cap configurable).
- Week 2 Day 12–13: E2E & API — offline E2E checked; DS→OCR bridge + API persistence still open.

## Decisions & Blockers
- Decision: Default to CUDA EP with a modest cap (e.g., 512–768 MB) when stability is a concern; use CPU if GPU pressure remains high. For maximum performance, set cap to 0.
- Blockers: None new; next priority remains DS bridge and API persistence.

## Next Actions
1) Validate on NX with: `--onnx-provider cuda --onnx-gpu-mem-limit-mb 512`; if OOM persists, switch to `--onnx-provider cpu`.
2) Implement DS→OCR (HTTP baseline) and API writes (SQLite + snapshots) per Week 2 Day 12–13.

## Sync Check
- README and plan are updated to reflect the new memory controls and recommended usage. Repository state matches this session’s changes.

