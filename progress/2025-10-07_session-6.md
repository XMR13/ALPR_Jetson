## Session Progress - 2025-10-07 (Session 6)

## Summary
- Marked Week 0 items as done per your update: the 10–20 short clips/frames were sourced from IT (no local capture needed) and CVAT is already hosted in WSL.
- Confirmed our repository remains aligned with plan.md §5; added remaining baseline configs/utilities earlier today.

## Changes
- Added:
  - `configs/ocr/postproc_indonesia.yaml` — OCR regex/whitelist/ambiguity rules per plan.
  - `configs/api/server.yaml` — baseline FastAPI server config (host/port/CORS/storage).
  - `tools/capture_frames.py` — optional RTSP frame capture tool (not needed now but kept for future use).
- Modified:
  - (none in this session)
- Deleted:
  - (none)

## Plan Snapshot (from plan.md §5)

Week 0 — Environment Bring‑Up
- Day 1: Flash NX to JetPack 5.1.5; verify toolchain — checked (per prior confirmations)
- Day 1: Set performance mode; Docker + NVIDIA runtime — open
- Day 2: Create repo using directory skeleton; push initial commit — checked (skeleton present)
- Day 2: Record 10–20 short clips and frames — checked (provided by IT; no capture needed)
- Day 3: Stand up CVAT; draft postproc_indonesia.yaml — checked (CVAT hosted in WSL; postproc YAML added)

Week 1 — Data Bootstrapping & Baselines
- Day 4–5: Label 2k frames; export COCO — open (awaiting export details/paths)
- Day 4–5: Generate OCR crops from boxes — open
- Day 6–7: Train YOLOX‑s; build TRT FP16 — open
- Day 6–7: Wire DeepStream pipeline + crop probe — open (code skeleton exists)

Week 2 — OCR Path + E2E V0
- Day 8–9: Integrate PaddleOCR (TRT) — open (skeleton exists)
- Day 10–11: Rectification + CLAHE preprocessing — open
- Day 12–13: FastAPI API + SQLite + snapshots — open (stub exists)
- Day 12–13: E2E test RTSP → DS → OCR → API — open

## Decisions & Blockers
- No capture required. Need dataset location(s) and CVAT export format to proceed with conversion and training steps.
- AGENTS.md says `config/` whereas repo uses `configs/`; we will continue with `configs/` for consistency with plan.md and current tree.

## Next Actions
- You: provide dataset paths (clips/frames) and CVAT project export details (format, label schema). If already exported, share the COCO/JSON location.
- Me: run `tools/coco_from_cvat.py` on provided exports, generate initial OCR crops via `tools/crop_from_boxes.py`, and update plan statuses accordingly.

## Sync Check
- Cross‑checked against plan.md §5 and current repo state. Session log updated to reflect tasks marked as done based on your confirmation.

