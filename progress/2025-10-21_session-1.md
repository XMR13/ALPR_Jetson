## Session Progress - 2025-10-21 (Session 1)

## Summary
- Captured TensorRT engine metadata structure: added per-engine configs for CCT-S and NVIDIA LPRNet and documented them in `docs/OCR_MODEL.md`. Created charset file for CCT-S to support Indonesian plates and scaffolded log destinations so Jetson-side `trtexec` outputs stay versioned but separated from the main doc.

## Changes
- Added:
  - `models/ocr/cct_charset.txt` — charset (0–9, A–Z) for the CCT-S OCR engine.
  - `configs/ocr/ocr_runtime_lprnet.yaml` — runtime config referencing `LPR_net.engine` (96×48, 3-channel, NCT layout).
  - `docs/build_logs/README.md` — instructions for storing `trtexec` outputs in dedicated log files.
  - `docs/build_logs/2025-10-21_trtexec_cct_s.log` — placeholder to paste Jetson `trtexec --dumpProfile` output for `cct_s.engine`.
  - `docs/build_logs/2025-10-21_trtexec_LPR_net.log` — placeholder to paste Jetson `trtexec --dumpProfile` output for `LPR_net.engine`.
- Modified:
  - `configs/ocr/ocr_runtime.yaml` — now targets `cct_s.engine` with explicit charset path, logits layout, channel count, and CLAHE defaults.
  - `docs/OCR_MODEL.md` — documents both OCR engines and points to the new build log directory.
- Deleted:
  - (none)

## Plan Snapshot (plan.md §5)
- Week 1 Day 6–7: Integrate YOLOv9 LPD ONNX/TRT; prepare DeepStream configs — checked (unchanged).
- Week 2 Day 8–9: Train/integrate OCR recognizer; build FP16 TRT engine — in_progress (engines present; profiling/testing on Jetson pending with new configs/log scaffolding).
- Week 2 Day 10–11: Add rectification + CLAHE preprocessing — checked (preprocess pipeline already in place).
- Week 2 Day 12–13: Choose DS→OCR transport; implement bridge/back-pressure; expand API; run E2E test — in_progress (transport decision outstanding; configs ready for OCR runtime selection).

## Decisions & Blockers
- Decision: Await user confirmation on preferred DS→OCR transport (HTTP vs ZeroMQ) before wiring the bridge.
- Blocker: Need Jetson-side `trtexec` logs and inference smoke tests to validate bindings and latency for both engines.

## Next Actions
1) Capture `trtexec --dumpProfile` outputs on the Jetson for both OCR engines and paste into the new log files.
2) Run `python -m alpr_jetson ocr-infer ...` against Jetson crop sets for each engine to compare CER/latency.
3) Decide which OCR engine becomes the default and report back so runtime/app wiring can follow.

## Sync Check
- Confirmed alignment with `plan.md` Week 2: OCR runtime assets are staged, DeepStream bridge remains pending transport decision, and documentation points to log evidence for upcoming Jetson tests.
