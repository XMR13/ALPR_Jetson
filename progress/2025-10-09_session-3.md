## Session Progress - 2025-10-09 (Session 3)

## Summary
- Switched the Kaggle notebook to use the official YOLOX repository with CUDA-enabled PyTorch installation. The notebook now clones YOLOX, installs Torch CUDA and dependencies, materializes our tuned experiment, trains (FP16), evaluates the best checkpoint, and exports ONNX plus metrics to `/kaggle/working/export/`.

## Changes
- Added:
  - (none)
- Modified:
  - `tools/kaggle/yolox_plate_train.ipynb` — clone official YOLOX, install Torch CUDA (cu121 fallback cu118), install YOLOX (editable), run train (FP16), run eval, parse COCO metrics, export ONNX and `best_ckpt.pth` to `/kaggle/working/export/`.
  - `tools/kaggle/README.md` — documented official-repo flow with CUDA wheels, validation section (metrics.json), troubleshooting, and clarified outputs.
- Deleted:
  - (none)

## Plan Snapshot (from plan.md Section 5)

Week 1 — Data Bootstrapping & Baselines
- Day 4–5: Label 2k frames; export COCO — checked
- Day 4–5: Generate OCR crops from boxes — checked
- Day 6–7: Train YOLOX; build TRT FP16 — in_progress (Kaggle notebook now trains via official repo + CUDA, exports ONNX and metrics)
- Day 6–7: Evaluate on val; log metrics — in_progress (metrics.json produced in Kaggle export)
- Day 6–7: Wire DeepStream pipeline + crop probe — open

Week 2 — OCR Path + E2E V0
- Day 8–9: Integrate PaddleOCR (TRT) — open (skeleton exists)
- Day 10–11: Rectification + CLAHE preprocessing — open
- Day 12–13: FastAPI API + SQLite + snapshots — open (stub exists)
- Day 12–13: E2E test RTSP + DS + OCR + API — open

## Decisions & Blockers
- Decision: Standardize Kaggle training on the official YOLOX repository to reduce drift and simplify dependency management; keep our tuned exp as a portable file.
- No blockers; next steps depend on model export and engine build on Jetson.

## Next Actions
- Run the updated Kaggle notebook (`tools/kaggle/yolox_plate_train.ipynb`) with GPU and Internet ON to produce `export/yolox_s.onnx`, `best_ckpt.pth`, and `metrics.json`.
- Transfer ONNX to Jetson and build FP16 TensorRT using `tools/trtexec_build.sh`.
- Validate detector on Jetson and begin integrating `src/deepstream_app/crop_probe.cpp`.

## Sync Check
- Progress remains aligned with `plan.md` Section 5. Training and validation tasks for Week 1 are now streamlined on Kaggle with clear artifacts and metrics.

